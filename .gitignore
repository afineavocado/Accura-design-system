# Dependencies
node_modules/

# Build output
.next/
out/
build/
storybook-static/
*.tsbuildinfo

# Environment — never commit; accura-ui/.env holds a real Figma token
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Build artifact — regenerate with: cd tokens && node sd.build.mjs
# Tracked until 2026-09-17, when it was found three tokens behind the export.
tokens/output/
