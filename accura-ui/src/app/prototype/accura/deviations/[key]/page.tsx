import { Unbuilt } from "../unbuilt"

export default function DeviationDetailPage() {
  return (
    <Unbuilt
      name="The deviation detail page"
      detail="§4–§6, including the six-step lifecycle header, the accumulating data blocks and the multi-signature gateway. The registry is the only screen built so far."
    />
  )
}
